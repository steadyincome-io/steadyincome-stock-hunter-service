name: Deploy GCP trading-assistant infra

on:
  push:
    branches: [main]
    paths:
      - "infra/**"
      - "!infra/**/*.md"
      - ".github/workflows/infra-deploy.yml"
  workflow_dispatch: {}

concurrency:
  group: infra-deploy
  cancel-in-progress: false

# id-token: write is what lets GitHub mint the OIDC token that GCP's
# Workload Identity Federation trusts -- no long-lived JSON key is stored
# as a secret anywhere in this repo.
permissions:
  contents: read
  id-token: write

jobs:
  terraform:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: infra/terraform
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: "projects/444713174784/locations/global/workloadIdentityPools/github-pool/providers/github-provider"
          service_account: "github-actions-deployer@stock-hunter-trading.iam.gserviceaccount.com"

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.13.3"

      - name: Terraform Init
        run: terraform init -input=false

      - name: Terraform Plan
        run: terraform plan -input=false

      - name: Terraform Apply
        run: terraform apply -input=false -auto-approve

      # position_monitor was decommissioned (see infra/MANUAL_SETUP.md
      # "Decommissioning") -- the post-apply smoke-test steps that used to
      # live here called its URL, which no longer exists (no outputs are
      # defined anymore). Removed rather than left broken. If GCP-hosted
      # infra is ever added back to this repo, a smoke-test step can be
      # rebuilt the same way (see git history for the removed version).
